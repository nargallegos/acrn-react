apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acrn.fullname" . }}-test-connection"
  labels:
    {{- include "acrn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acrn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
