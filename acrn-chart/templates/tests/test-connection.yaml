apiVersion: v1
kind: Pod
metadata:
  name: {{ .Release.Name }}-test-connection
  labels:
    {{ .Release.Name }}-acrn
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ["{{ .Values.ingress.hostname }}:3000"]
  restartPolicy: Never
